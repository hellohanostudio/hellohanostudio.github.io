{
  "name": "hanostudio-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist/server dist/.openai && cp index.html hano-128.png hano-mark.svg dist/ && cp .openai/hosting.json dist/.openai/hosting.json && cp server/index.js dist/server/index.js"
  }
}
